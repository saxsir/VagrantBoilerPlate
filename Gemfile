# A sample Gemfile
source "https://rubygems.org"

# サーバー構成のテストツール
gem "serverspec"

# cookbookつくったりするのにローカルにも必要
# knife-solo入れたら一緒にインストールされる
# gem "chef"

# chef-solo環境でchefを使うためのユーティリティツール
# knife solo init が便利だったから追加
gem "knife-solo"
